# TODO

## Completed Today

- 첫 화면 온보딩 추가
- 초보자용 결과물 미리보기 추가
- 용어 번역 사전 추가
- 단계명을 초보자용 표현으로 변경
- 질문 도움말을 쉬운 말투로 정리
- 10단계 완료 후 결과 화면으로 이동하는 구조 추가
- 결과 화면 탭 UI 추가
- 결과 화면 상단/하단 광고 슬롯 자리 추가
- Wizard 진행 중 하단 전체 프롬프트 영역을 광고 슬롯 자리로 교체
- 광고용 환경변수 예시 추가
- 깨져 있던 주요 UI/프롬프트 한글 문구 정리

## Next TODO

## 1. 이어하기를 여러 프로젝트 선택 기능으로 확장

현재 `이어하기`는 마지막으로 저장된 단일 프로젝트를 Wizard 화면에서 계속 작성하게 합니다. 다음 작업에서는 여러 개의 작성 중인 아이디어를 저장하고, 사용자가 목록에서 하나를 클릭해 이어서 볼 수 있게 만듭니다.

작업:

- LocalStorage 저장 구조를 단일 프로젝트에서 여러 프로젝트 배열로 확장
- `SavedProject` 타입 추가
- `activeProjectId` 저장
- 기존 v1 저장값을 v2 구조로 마이그레이션
- `이어하기` 버튼 클릭 시 저장된 아이디어 목록 표시
- 프로젝트 카드에 서비스 이름, 마지막 수정 시간, 현재 단계, 완료율 표시
- 프로젝트 카드 클릭 시 해당 입력값과 현재 단계 로드
- 저장된 프로젝트가 없을 때 빈 상태 안내 표시

완료 기준:

- 여러 개의 프로젝트 초안을 저장할 수 있다.
- `이어하기`에서 저장된 프로젝트 중 하나를 선택해 열 수 있다.
- 기존 사용자의 단일 저장 데이터가 사라지지 않는다.
- 초보자도 “작성 중인 아이디어를 고른다”는 흐름을 이해할 수 있다.

## 2. 상단 버튼 피드백 정리

상단 버튼은 기능이 있어도 사용자가 결과를 바로 느끼지 못하면 헷갈립니다. 각 버튼에 명확한 피드백을 추가합니다.

작업:

- `저장` 클릭 후 저장 완료 상태 표시
- `내보내기` 클릭 후 복사 완료 상태 표시
- `새 프로젝트` 클릭 전 확인 모달 추가
- `도움말` 클릭 시 용어 사전 또는 도움말 패널 열기

완료 기준:

- 버튼을 누른 뒤 무슨 일이 일어났는지 화면에서 바로 알 수 있다.
- 실수로 작성 중인 내용을 날리지 않는다.

## 3. 결과 화면 완성도 높이기

현재 결과 화면은 탭 구조와 복사 기능의 기본 틀이 있습니다. 다음 작업에서는 각 탭의 문서 품질을 더 높입니다.

작업:

- 요약서 문장 다듬기
- 처음 만들 기능 명세를 더 구체화
- 화면 구조 탭에 화면별 역할 추가
- 만들 방법 탭에 초보자용 기술 설명 추가
- AI에게 시킬 말 탭을 Codex/Cursor/Claude용으로 분리할지 검토

완료 기준:

- 각 탭이 그대로 복사해도 쓸 수 있는 문서처럼 보인다.
- “아직 입력되지 않음”이 많은 경우에도 안내 문구가 어색하지 않다.

## 4. Markdown 다운로드 기능 추가

복사만으로는 부족하므로 결과 탭을 `.md` 파일로 내려받을 수 있게 합니다.

작업:

- 현재 탭 Markdown 다운로드
- 전체 결과물 Markdown 다운로드
- 파일명 생성 규칙 추가

완료 기준:

- 브라우저에서 Markdown 파일이 다운로드된다.
- 파일명에 서비스 이름이 반영된다.

## 5. 온보딩 세부 UX 개선

현재 온보딩은 설명과 결과물 미리보기를 보여줍니다. 다음에는 사용자의 불안을 더 줄이는 문구를 추가합니다.

추가 후보:

- “정답은 없습니다”
- “선택 후 언제든 수정할 수 있습니다”
- “개발 지식이 없어도 됩니다”
- “끝나면 AI에게 붙여넣을 요청서가 생깁니다”

완료 기준:

- 첫 화면만 보고도 서비스 목적과 결과물을 이해할 수 있다.
- 시작하기 버튼이 눈에 잘 띈다.

## 6. 용어 도움말 UI 연결

용어 사전은 데이터로 추가되어 있습니다. 다음에는 Wizard 안에서 실제 도움말로 연결합니다.

작업:

- 어려운 용어 옆 `?` 버튼 추가
- 클릭 또는 hover 시 쉬운 설명 표시
- 모바일에서도 설명이 잘 보이도록 처리

완료 기준:

- MVP, API, 배포, 프롬프트 같은 단어를 화면에서 바로 설명할 수 있다.

## 7. 광고 슬롯 컴포넌트 고도화

현재는 placeholder 광고 슬롯입니다. 실제 적용 전 구조를 조금 더 단단히 합니다.

작업:

- `NEXT_PUBLIC_ADSENSE_CLIENT_ID` 값이 있을 때만 실제 광고 영역 준비
- 값이 없으면 placeholder 표시
- 진행 중 하단, 결과 상단/하단 슬롯 ID 분리

완료 기준:

- 광고 환경변수가 없어도 레이아웃이 깨지지 않는다.
- 실제 광고 ID를 `.env.local`에 넣을 수 있는 구조다.

## 8. 검증

작업 후 반드시 확인:

```bash
npm run lint
npm run build
```

브라우저 확인:

- 첫 화면
- 시작하기
- Wizard 단계 이동
- 선택지 클릭
- 결과 보기
- 결과 탭 전환
- 탭 복사
- 모바일 390px 화면

## Next Commit Message Candidate

```text
Add multi-project continue flow
```
